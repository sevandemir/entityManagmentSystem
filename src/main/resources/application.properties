# Server Settings
server.port=9090

# Database Connection (Ubuntu Server PostgreSQL)
spring.datasource.url=jdbc:postgresql://100.87.129.49:5432/eventhub
spring.datasource.username=postgres
spring.datasource.password=Musab.21
spring.datasource.driver-class-name=org.postgresql.Driver
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect
spring.data.web.pageable.page-serialization-mode=via-dto

# JPA / Hibernate Settings
spring.jpa.hibernate.ddl-auto=validate
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true
spring.jpa.open-in-view=false

# Flyway Settings
spring.flyway.enabled=true
spring.flyway.baseline-on-migrate=true
spring.flyway.locations=classpath:db/migration

# Logging
logging.level.org.flywaydb=DEBUG
logging.level.org.eventhub=DEBUG
logging.level.org.springframework.security=DEBUG

# File Upload Settings
spring.servlet.multipart.max-file-size=5MB
spring.servlet.multipart.max-request-size=5MB

# Security & JWT
jwt.secret=bu-benim-cok-guclu-ve-sabit-gizli-anahtarim-2026-eventhub
jwt.expiration=900000
jwt.refresh-expiration=604800000

# File Path
file.upload-dir=uploads/

# CORS
cors.allowed-origins=http://localhost:5173

# Redis (Token Blacklist için)
spring.data.redis.host=localhost
spring.data.redis.port=6379
spring.data.redis.timeout=2000ms